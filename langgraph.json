{
  "$schema": "https://langgra.ph/schema.json",
  "python_version": "3.12",
  "graphs": {
    "agent": "agent.server.graph:get_agent"
  },
  "dependencies": ["."],
  "http": {
    "app": "agent.server.webapp:app"
  },
  "env": ".env" 
}
